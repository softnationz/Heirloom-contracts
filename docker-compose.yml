version: '3.9'

services:
  db:
    image: postgres:16-alpine
    container_name: heirloom-protocol-db
    environment:
      POSTGRES_USER: heirloom_user
      POSTGRES_PASSWORD: heirloom_password
      POSTGRES_DB: heirloom_protocol
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U heirloom_user -d heirloom_protocol"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: heirloom-protocol-backend
    environment:
      DATABASE_URL: postgres://heirloom_user:heirloom_password@db:5432/heirloom_protocol
      REDIS_URL: redis://redis:6379
      NODE_ID: backend-primary
      CONSENSUS_STRATEGY: last_write_wins
      RUST_LOG: info
    ports:
      - "3000:3000"
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./backend/src:/app/src

  redis:
    image: redis:7-alpine
    container_name: heirloom-protocol-redis
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  stellar-quickstart:
    image: stellar/quickstart:latest
    container_name: stellar-quickstart
    environment:
      NETWORK_PASSPHRASE: "Test SDF Network ; September 2015"
    ports:
      - "8000:8000"
      - "11626:11626"
    volumes:
      - quickstart_data:/var/lib/stellar

volumes:
  postgres_data:
  quickstart_data:
